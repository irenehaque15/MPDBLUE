<% form_for @student, :as => :student, :url=>student_path(@student), :html=>{:method=>:put} do |form| %>
	<%= collection_select(:student, :team_id, Team.all, :id, :name) %>
	<%= submit_tag "update" %>
<% end %>
